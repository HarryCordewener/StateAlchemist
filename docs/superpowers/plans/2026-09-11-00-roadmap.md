# StateAlchemist roadmap

**Spec:** [`../specs/2026-09-11-statealchemist-design.md`](../specs/2026-09-11-statealchemist-design.md) ·
**Docs:** [`../../index.md`](../../index.md)

The order of work is chosen so architectural problems surface while they are cheap to fix:

1. **Documentation first.** The user docs describe the library as designed. They are the contract every later
   step is checked against, and the first place a design problem shows: if a behaviour is hard to explain, it is
   wrong.
2. **Interface-based tests next.** The semantics become tests written against `IMachine<TValue>` — not against
   any implementation — and run first against a slow, obviously-correct **reference interpreter**.
3. **The generator last.** It is built to make the same contract suite pass. A generated machine that disagrees
   with the interpreter fails the same test.

## The plans

| # | Plan | Delivers | Status |
|---|---|---|---|
| — | Documentation | `docs/`: concepts, guides, reference | **Done** — in review |
| 1 | [Foundation](2026-09-11-01-foundation.md) | Repository, CI, public API with XML docs and an API snapshot test, docs pipeline, the semantic model and its analysis, the reflection front-end | **Written and validated**: rebuilt from the plan text alone, 345 tests pass |
| 2 | [Reference interpreter and contract suite](2026-09-11-02-reference-and-contracts.md) | `IMachine<TValue>` by reflection over the model; the contract suite for states, transitions, triggers, actions, lifecycle and exceptions | **Written and validated**: rebuilt from Plans 1–2's text alone, 507 tests pass |
| 3 | [Decisions, deferral, runs, concurrency — interpreted](2026-09-11-03-decisions-runs-concurrency.md) | The same for the hardest semantics: pending states, cancellation, backpressure, runs, the three concurrency modes | **Written and validated**: rebuilt from Plans 1–3's text alone, 597 tests pass, and 15 repeated runs without a failure |
| 4 | [The source generator](2026-09-11-04-generator.md) | The Roslyn front-end, every diagnostic in the compiler, and the emitter: generated machines with actions, events, lifecycle, hooks and exceptions, passing Plan 2's contract suite; agreement with the interpreter on random machines | **Written and validated**: rebuilt from Plans 1–4's text alone, 822 tests pass |
| 5 | [Generated decisions, deferral, runs, `Serialized`](2026-09-11-05-generated-decisions-runs.md) | The interpreter's inbox and pump, generated where needed; decisions and their pending state; runs; all 76 contracts on generated machines | **Written and validated**: rebuilt from Plans 1–5's text alone, 909 tests pass, stable over repeated runs |
| 6 | [Performance](2026-09-11-06-performance.md) | Flattened entry points, pooled inbox inputs and an inline path for an idle caller, `InboxCapacity`, allocation tests on every synchronous path, the benchmark suite against Stateless and a hand-written `switch`, generation and construction on a TNC-sized machine, a `PublishAot` sample in CI | **Written and validated**: rebuilt from Plans 1–6's text alone, 939 tests pass; every spec §9 target met |
| 8 | [Exported modules and the package](2026-09-11-08-exported-modules-and-the-package.md) | D25: a library offers a module with `[assembly: ExportsModule]`, a machine takes what its references offer with `[IncludeExported]`; `SALCH0108`/`SALCH0109`; the version from the git tag, symbols, package validation, and a release workflow that publishes without a stored key | **Written and validated**: rebuilt from Plans 1–8's text alone, 1,077 tests pass; nothing published |
| 7 | [Preview](2026-09-11-07-preview.md) | The declaration-site analyzer and the code fixes (D24 included), `SALCH0801` over a method's control flow, a synchronous `Fire` with `SALCH0601` at the call, `Mermaid` and `Dot` constants, the documentation checked against the code, the package built and consumed in CI, the [TNC 4.0 migration spec](../specs/2026-09-11-tnc-4.0-migration-design.md) | **Written and validated**: rebuilt from Plans 1–7's text alone, 1,047 tests pass; the package is used by an application that references nothing else |

The code of every validated plan is on `main`, one commit per plan, so the repository always holds what the plans
say. Publishing a package is a decision for its time, not a step of any plan: Plan 7 builds one and uses it in CI.

Each plan argues from the types and lessons of the one before it, so none is written against guesses: its code is
first built and tested on top of the previous plans' validated tree, and only then written down. A plan counts as
validated when its files, extracted from the plan text alone on top of the earlier plans, build and pass. The gates
below remain review stops before *executing* the next plan.

## How the tests are layered

| Layer | Tests | Catches |
|---|---|---|
| Public API | A snapshot of the runtime's entire public surface (PublicApiGenerator + Verify) | Any unintended API change, in review |
| Model | Unit tests of the analysis on small hand-built trees | Wrong semantics — LCA, roles, precedence, conflicts — with no machine involved |
| Contract | One suite against `IMachine<TValue>`, inherited by a test project per implementation (`[InheritsTests]`) | Any implementation that disagrees with the docs |
| Allocation and benchmarks | `GC.GetAllocatedBytesForCurrentThread` tests on every synchronous path; BenchmarkDotNet against Stateless and a hand-written `switch` | Performance regressions |

## Gates

Each gate is a stop for review before the next plan is written.

- **After documentation:** the docs read as a library you would want to use; every concept is explained without
  hand-waving.
- **After Plan 1:** the model's API and the analysis tests settle every rule in the docs. Anything the model cannot
  express is a design change, made in the spec and docs first.
- **After Plan 2:** the contract suite passes on the reference interpreter, and the TNC-shaped sample machine
  expresses every pattern TNC needs.
- **After Plan 3:** decisions, deferral and concurrency behave as documented under test, including cancellation and
  backpressure against a real `Pipe`.
- **After Plan 4:** generated machines pass the Plan 2 contract suite and agree with the interpreter on random
  machines; the generated code reads as spec §7 describes it.
- **After Plan 5:** the same for Plan 3's contracts.
- **After Plan 6:** every performance target in spec §9 is met.
- **After Plan 7:** a library's own mistakes are reported where the library is written, the package works for an
  application that references nothing else, and the TNC migration has a design to argue with.
- **After Plan 8:** adding a protocol is adding a package reference, and a `v0.1.0` tag is all that stands between
  the repository and a release.

## Findings from validating Plan 1

Building Plan 1's first tasks in a scratch copy surfaced these; the plan already reflects them.

- **`dotnet sln add` needs `--include-references false`** on the .NET 11 SDK. It otherwise re-adds referenced
  projects and fails with "Solution folder 'src' already contains a project".
- **Verify tags *received* snapshots by framework** (`RuntimeApi.DotNet11_0.received.txt`) while the *verified*
  file is shared. Accept by renaming one received file to `RuntimeApi.verified.txt`.
- **`dotnet new tool-manifest` writes `dotnet-tools.json` at the repository root** on this SDK, not in `.config/`.
- **MarkdownSnippets** needs `"ExcludeMarkdownDirectories": ["superpowers"]` (a directory *name*) in
  `mdsnippets.json` to leave specs and plans alone; the command-line flag did not work.
- **A transition method named like a constant it uses** (`Will` with `[On(Will)]`) breaks the attribute. Now in
  the docs as a pitfall.
- **Role consistency is per binding, not per role.** A transition declared on a parent may name a state if it
  *binds* the same way from every leaf. The first rule compared raw roles and wrongly refused `ref Naws` on a
  transition into `Naws` from its parent. Fixed in the spec and docs.

## Findings from validating Plan 2

Running the contract suite on the reference interpreter surfaced these; the spec and docs already reflect them.

- **A state being started over is active when step 2 resets it**, so its snapshot is taken first and put back if
  the transform throws. Spec §6.2 had claimed step 2 only touches inactive slots.
- **`SALCH0301` does not apply to the root**: re-entering it clears nothing.
- **`Enqueue` outside a transition throws**, and **`StartAsync` twice throws** — both previously undefined.
- **Events queued before a rethrow run before the next trigger.**

## Findings from validating Plan 3

The decision, backpressure, run and concurrency contracts surfaced these; the spec and docs already reflect them.

- **`DecisionFailed` is a runtime type**, not generated: a module declares its recovery before any machine exists.
- **The pending state sits below the active leaf** (a decision plans as a stay); decisions' `Decide` and per-outcome
  `Completed` are now role-checked the way they run.
- **A failed decision — `Decide` or `DecideAsync` — fires `DecisionFailed` from the leaf after the pending state
  ends.** Events that waited run when the decision resolves, before the rest of the paused input.
- **A batch that throws** stops there and cancels a decision it started.
- **Self-firing is caught in every transition while a decision is pending**, at no cost otherwise; `Enqueue` from a
  decision must start processing, because the machine is idle while a decision runs.
- **The generated machine's inbox design works for every mode**: the interpreter uses it throughout, with the
  modes differing only in what they refuse.

## Findings from validating Plan 4

- **Plans 4–7 are re-cut.** Actions, events, lifecycle, hooks and exceptions moved into Plan 4: nearly every
  contract machine has actions, so a generator without them could not be held to the suite. Performance became its
  own plan (6), so Plan 5 is about semantics alone.
- **Roslyn cannot see a referenced assembly's non-public members**, so the declaring-library diagnostics need an
  analyzer where the module is written (Plan 7).
- **Generated code is plain C# 7.3**, compiled in a test at that version with every warning reported.

## Findings from validating Plan 5

- **Plan 4's refusal of what it could not generate did not compile** under warnings-as-errors; fixed in its text.
- **The inbox is generated only where it is needed**, and its decision parts only where there is an async decision:
  unused generated fields are warnings, and so errors.
- **The bounded inbox (`InboxCapacity`) moves to Plan 6**: neither implementation honours it yet.

## Findings from validating Plan 6

Measuring the generated machine surfaced these; the spec and docs already reflect them.

- **The `Channel` inbox was the wrong guess.** A list and a lock, with the caller's own input as the completion
  source, is both simpler and faster: a channel's completion sources and its bounded mode cost more than the rest
  of the call, and the machine has one reader by construction. `InboxCapacity` is a `SemaphoreSlim` counting room.
- **A caller that finds the machine idle should run its own input inline.** Waking a pump to do what the calling
  thread could do halved the cost of a `Serialized` call (141 ns → 71 ns) and removed the thread hop.
- **"Not started and stopped cost nothing" was not true.** They cost one comparison. It is below what a benchmark
  can measure, which is the honest way to say it.
- **A hand-written baseline can vanish.** A stay written by hand inlines to a field increment that neither the JIT
  nor BenchmarkDotNet can distinguish from an empty method, so "within 2× of hand-written" is only a number where
  the baseline does measurable work — the 1 KB run, where the generated machine is 1.7×.
- **A suspended call should finish in one `async` method.** It finished in two, and each is a state machine the
  caller pays for; collapsing them took a suspending call from 470 B to 216 B in Release.
- **Construction must not allocate.** A machine with runs built its one-element run buffer in its constructor; it
  is built on first use instead, so construction allocates the instance and nothing else.
- **Per-thread allocation counters cannot measure asynchronous work.** A continuation resumes on another thread, so
  the suspending allocation test installs a synchronization context and pumps the continuations itself.

## Findings from validating Plan 8

- **An assembly attribute must precede every type in its file.** Both the exporting library and the tests learned
  this the same way: an `[assembly: ExportsModule]` written after a file-scoped namespace is `CS1730`. A library
  puts it in `AssemblyInfo.cs`, and the tests' helper now fails when its own source does not compile — a test that
  did not look proved nothing.
- **A test project that references an exporting library changes every in-source test in it.** The generator tests
  compile source against every assembly the test process loaded, so the cross-assembly case belongs to the
  generated test project, where a real machine is built, and the in-source tests stay hermetic.
- **The two front-ends see "referenced assemblies" differently**, and it does not matter: Roslyn sees every
  reference passed to the compiler, reflection sees the ones the emitted assembly records. A module that is
  actually included is called by the generated code, so the reference is recorded and they agree.
- **MinVer is the version, and a tree with no git history is a real case.** A source tree extracted from these
  plans builds `0.0.0-preview.0`; that is the right answer there, so `MINVER1001` is a message rather than a
  warning.
- **Deterministic source paths belong only where a package ships.** Turning `ContinuousIntegrationBuild` on for
  the whole repository on CI rewrote `[CallerFilePath]` to `/_/…`, and the tests that read the documentation could
  no longer find it. Source Link needs it in the package; nothing else does.
- **Package validation needs a published version to diff against.** Until the first release it only checks the
  package's frameworks against each other, and `PackageValidationBaselineVersion` is set the moment a version
  lands.

## Findings from validating Plan 7

- **An analyzer is not optional for a library's own mistakes.** Roslyn cannot see a referenced assembly's
  non-public members, so a module compiled into a library would otherwise be checked only when an application
  assembled a machine — and then reported in the wrong place. The analyzer builds the model of one module, with no
  root and no value type, and reports only what that can prove.
- **A diagnostic has to point where its fix applies.** `SALCH0201` pointed at the method and `SALCH0206` at the
  class; both now point at the parameter and the method, which is what let the code fixes find them.
- **`Fire` exists on every machine.** The spec had it appear only on machines with nothing async, but the generator
  writes one machine at a time and cannot know how a caller means to use it — so it is always written, and
  `SALCH0601` is reported at the call. The batch overload takes `ReadOnlyMemory`, not a span.
- **A fix cannot invent a union**, so a decision's `Decide` and `Complete` are not offered by "add a phase".
- **`ControlFlowGraph.Create` refuses a method body's block**, which has a parent; the analysis context hands out
  the graph instead.
- **Test for `AD0001`.** An analyzer that throws looks exactly like one that found nothing, so every analyzer test
  asserts that Roslyn did not report a crash.
- **What is left of "every docs sample compiled"**: the snippet-driven blocks are compiled by being taken from the
  samples; the hand-written ones are parsed, not compiled. Moving them into the samples is worth doing when the
  prose settles.
